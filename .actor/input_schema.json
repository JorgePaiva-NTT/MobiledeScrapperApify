{
    "title": "Input schema of the MobileDeScrapper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "url": {
            "title": "Mobile.de vehicle URL",
            "type": "string",
            "description": "Single vehicle ad URL to scrape.",
            "editor": "textfield",
            "prefill": "https://suchen.mobile.de/auto-inserat/porsche-992-911-carrera-gts-cabrio-bose-ha-lenkung-21-bielefeld/444370284.html"
        },
        "proxyCountry": {
            "title": "Proxy country",
            "type": "string",
            "description": "Optional ISO country code for Apify Proxy, e.g. DE.",
            "editor": "textfield"
        },
        "proxyGroup": {
            "title": "Proxy group",
            "type": "string",
            "description": "Preferred Apify Proxy group. Actor will automatically fallback to DATACENTER if the selected group is unavailable.",
            "editor": "select",
            "default": "RESIDENTIAL",
            "enum": ["RESIDENTIAL", "DATACENTER"]
        },
        "maxRequestRetries": {
            "title": "Max request retries",
            "type": "integer",
            "description": "How many retries to do if Mobile.de blocks a request.",
            "default": 4
        }
    },
    "required": ["url"]
}